{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "k8s-dra-driver-npu.fullname" . }}-webhook-config
  labels:
    {{- include "k8s-dra-driver-npu.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
  annotations:
    cert-manager.io/inject-ca-from: "{{ include "k8s-dra-driver-npu.namespace" . }}/{{ include "k8s-dra-driver-npu.fullname" . }}-webhook-cert"
webhooks:
- name: {{ include "k8s-dra-driver-npu.driverName" . | quote }}
  rules:
  - apiGroups:   ["resource.k8s.io"]
    apiVersions: ["v1beta1", "v1beta2", "v1"]
    operations:  ["CREATE", "UPDATE"]
    resources:   ["resourceclaims", "resourceclaimtemplates"]
    scope:       "Namespaced"
  clientConfig:
    service:
      namespace: {{ include "k8s-dra-driver-npu.namespace" . }}
      name: {{ include "k8s-dra-driver-npu.fullname" . }}-webhook
      port: {{ .Values.webhook.servicePort }}
      path: /validate-resource-claim-parameters
  admissionReviewVersions: ["v1"]
  sideEffects: None
{{- end }}
